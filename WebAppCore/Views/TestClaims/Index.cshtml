@{
    ViewData["Title"] = "Тестування Claims";
}

<h1>@ViewData["Title"]</h1>

<div class="alert alert-info">
    <strong>Примітка:</strong> Ця сторінка призначена для тестування Claims.
    Після зміни Claim потрібно <strong>вийти та увійти знову</strong>.
</div>

<h2>Поточні Claims користувача: @ViewBag.UserEmail</h2>

<table class="table table-bordered">
    <thead>
        <tr>
            <th>Тип Claim</th>
            <th>Значення</th>
        </tr>
    </thead>
    <tbody>
        @if (ViewBag.Claims != null)
        {
            foreach (var claim in ViewBag.Claims)
            {
                <tr>
                    <td><strong>@claim.Type</strong></td>
                    <td>@claim.Value</td>
                </tr>
            }
        }
        else
        {
            <tr>
                <td colspan="2">Немає claims</td>
            </tr>
        }
    </tbody>
</table>

@if (TempData["SuccessMessage"] != null)
{
    <div class="alert alert-success">@TempData["SuccessMessage"]</div>
}

@if (TempData["ErrorMessage"] != null)
{
    <div class="alert alert-danger">@TempData["ErrorMessage"]</div>
}

<hr />

<!-- --------------------------------------------------------
     EXISTING WorkingHours form
-------------------------------------------------------- -->

<h3>Змінити WorkingHours Claim</h3>
<p>Введіть нове значення для тестування Premium-доступу:</p>

<form asp-action="UpdateWorkingHours" method="post" class="mt-3">
    @Html.AntiForgeryToken()
    <div class="mb-3">
        <label for="hours" class="form-label">Кількість годин:</label>
        <input type="number" id="hours" name="hours" class="form-control" value="50" min="0" required />
    </div>
    <button type="submit" class="btn btn-primary">Оновити WorkingHours</button>
</form>

<hr />

<!-- --------------------------------------------------------
     НОВІ CLAIMS: IsMentor, IsVerifiedUser, HasForumAccess
-------------------------------------------------------- -->

<h3>Керування Forum Claims</h3>
<p>Ці claim-и впливають на доступ до сторінки <strong>Форуму</strong>.</p>

<!-- IsMentor -->
<div class="mb-4">
    <h5>IsMentor</h5>

    <form method="post" asp-action="AddClaim">
        <input type="hidden" name="type" value="IsMentor" />
        <input type="hidden" name="value" value="true" />
        <button class="btn btn-success">Додати IsMentor</button>
    </form>

    <form method="post" asp-action="RemoveClaim" class="mt-1">
        <input type="hidden" name="type" value="IsMentor" />
        <button class="btn btn-danger">Видалити IsMentor</button>
    </form>
</div>

<!-- IsVerifiedUser -->
<div class="mb-4">
    <h5>IsVerifiedUser</h5>

    <form method="post" asp-action="AddClaim">
        <input type="hidden" name="type" value="IsVerifiedUser" />
        <input type="hidden" name="value" value="true" />
        <button class="btn btn-success">Додати IsVerifiedUser</button>
    </form>

    <form method="post" asp-action="RemoveClaim" class="mt-1">
        <input type="hidden" name="type" value="IsVerifiedUser" />
        <button class="btn btn-danger">Видалити IsVerifiedUser</button>
    </form>
</div>

<!-- HasForumAccess -->
<div class="mb-4">
    <h5>HasForumAccess</h5>

    <form method="post" asp-action="AddClaim">
        <input type="hidden" name="type" value="HasForumAccess" />
        <input type="hidden" name="value" value="true" />
        <button class="btn btn-success">Додати HasForumAccess</button>
    </form>

    <form method="post" asp-action="RemoveClaim" class="mt-1">
        <input type="hidden" name="type" value="HasForumAccess" />
        <button class="btn btn-danger">Видалити HasForumAccess</button>
    </form>
</div>

<hr />

<div class="mt-3">
    <a asp-controller="Home" asp-action="Index" class="btn btn-secondary">На головну</a>
    <a asp-controller="Forum" asp-action="Index" class="btn btn-info">Перейти на Форум</a>
</div>
