@{
    ViewData["Title"] = "Мої улюблені рецепти";
}
@model IEnumerable<SlayLib.Models.Recipe>
@inject Microsoft.AspNetCore.Mvc.Localization.IViewLocalizer Localizer

<div class="container py-4" style="max-width: 1400px;">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="fw-bold" style="background: linear-gradient(135deg, #ff6b35 0%, #f7931e 100%); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text;">
            ❤️ @ViewData["Title"]
        </h1>
        <a asp-action="Index" class="btn btn-outline-secondary rounded-pill px-4 py-2">
            ← Назад до всіх рецептів
        </a>
    </div>

    @if (Model != null && Model.Any())
    {
        <div class="row g-4">
            @foreach (var recipe in Model)
            {
                <div class="col-md-6 col-lg-4">
                    <div class="card shadow-sm h-100 rounded-4" style="border: none; transition: transform 0.2s;">
                        @if (!string.IsNullOrEmpty(recipe.ImageUrl))
                        {
                            <img src="@recipe.ImageUrl" class="card-img-top" alt="@recipe.Name" style="height: 200px; object-fit: cover; border-radius: 1rem 1rem 0 0;" />
                        }
                        else
                        {
                            <div class="card-img-top d-flex align-items-center justify-content-center bg-light" style="height: 200px; border-radius: 1rem 1rem 0 0;">
                                <span style="font-size: 3rem;">🍳</span>
                            </div>
                        }
                        <div class="card-body d-flex flex-column">
                            <h5 class="card-title fw-bold">@recipe.Name</h5>
                            <p class="card-text text-muted small flex-grow-1">
                                @(recipe.Description.Length > 100 ? recipe.Description.Substring(0, 100) + "..." : recipe.Description)
                            </p>
                            <div class="mb-2">
                                <span class="badge rounded-pill me-1" style="background: linear-gradient(135deg, #ff6b35 0%, #f7931e 100%); color: white;">
                                    @GetCategoryName(recipe.Category)
                                </span>
                                <span class="badge rounded-pill bg-@GetDifficultyColor(recipe.Difficulty) me-1">
                                    @GetDifficultyName(recipe.Difficulty)
                                </span>
                                <span class="badge rounded-pill bg-secondary">⏱️ @recipe.PreparationTime хв</span>
                            </div>
                            <div class="mt-auto">
                                <a asp-action="Details" asp-route-id="@recipe.Id" class="btn btn-primary rounded-pill w-100">
                                    Переглянути деталі
                                </a>
                            </div>
                        </div>
                        <div class="card-footer bg-transparent border-top-0" style="padding-top: 0;">
                            <small class="text-muted">
                                👤 @(recipe.Author?.FirstName ?? "Невідомо") @(recipe.Author?.LastName ?? "")
                                <br />
                                📅 @recipe.CreatedAt.ToString("dd.MM.yyyy")
                            </small>
                        </div>
                    </div>
                </div>
            }
        </div>
    }
    else
    {
        <div class="alert alert-info text-center rounded-4">
            <h4>У вас поки що немає улюблених рецептів</h4>
            <p>Додайте рецепти до улюблених, натиснувши на ❤️ на сторінці деталей рецепту.</p>
            <a asp-action="Index" class="btn btn-primary rounded-pill px-4 py-2 mt-3">
                Переглянути всі рецепти
            </a>
        </div>
    }
</div>

@functions {
    string GetCategoryName(string category)
    {
        return category switch
        {
            "Breakfast" => "Сніданок",
            "Lunch" => "Обід",
            "Dinner" => "Вечеря",
            "Dessert" => "Десерт",
            "Snack" => "Закуска",
            _ => category
        };
    }

    string GetDifficultyName(string difficulty)
    {
        return difficulty switch
        {
            "Easy" => "Простий",
            "Medium" => "Середній",
            "Hard" => "Складний",
            _ => difficulty
        };
    }

    string GetDifficultyColor(string difficulty)
    {
        return difficulty switch
        {
            "Easy" => "success",
            "Medium" => "warning",
            "Hard" => "danger",
            _ => "secondary"
        };
    }
}

