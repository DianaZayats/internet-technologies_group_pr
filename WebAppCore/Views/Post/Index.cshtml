@{
    ViewData["Title"] = @Localizer["Title"];
}
@model IEnumerable<SlayLib.Models.Post>
@inject Microsoft.AspNetCore.Mvc.Localization.IViewLocalizer Localizer

<div class="container py-4 forum-container" style="max-width: 1200px;">

    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="fw-bold" style="background: linear-gradient(135deg, #ff6b35 0%, #f7931e 100%); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text;">
            💬 @ViewData["Title"]
        </h1>

        <a asp-action="Create" class="btn btn-gradient rounded-pill px-4 py-2">
            ➕ @Localizer["AddTopic"]
        </a>
    </div>

    <div class="card-modern">
        <div class="card-header fw-bold" style="background: linear-gradient(135deg, #ff6b35 0%, #f7931e 100%); color: white; border-radius: 20px 20px 0 0;">
            @Localizer["ForumTopics"]
        </div>

        <table class="table table-hover mb-0">
            <thead class="table-secondary">
                <tr>
                    <th style="width: 60%">@Localizer["Topic"]</th>
                    <th style="width: 20%">@Localizer["Created"]</th>
                    <th style="width: 20%" class="text-center">Дії</th>
                </tr>
            </thead>

            <tbody>

                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            <div class="fw-semibold">
                                <a asp-action="Details" asp-route-id="@item.Id" 
                                   class="text-decoration-none thread-title">
                                   @item.Title
                                </a>
                            </div>

                            <div class="text-muted small mt-1">
                                Автор: @(item.Author != null ? item.Author.FirstName + " " + item.Author.LastName : "Невідомо")
                            </div>
                        </td>

                        <td class="text-muted">
                            @item.CreatedAt.ToString("dd.MM.yyyy HH:mm")
                        </td>

                                    <td class="text-center">
                                        <a asp-action="Details" asp-route-id="@item.Id"
                                           class="btn btn-sm btn-gradient rounded-pill px-3">Читати</a>

                                        <a asp-action="Edit" asp-route-id="@item.Id"
                                           class="btn btn-sm btn-outline-secondary rounded-pill px-3 ms-2">Редаг.</a>
                                    </td>
                    </tr>
                }

            </tbody>
        </table>
    </div>

</div>

<style>
    .thread-title {
        font-size: 1.05rem;
        color: #0d6efd;
    }

    .thread-title:hover {
        text-decoration: underline;
    }

    .forum-container {
        max-width: 900px;
    }
</style>
